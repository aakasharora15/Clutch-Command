/**
 * Central CTA configuration.
 * Change these values once to update every button across the entire site.
 */
export const CTA = {
  url: 'https://stripe.com/gb/payments/onelink',
  label: 'Join the Academy',
  labelArrow: 'Join the Academy \u2192',
} as const;
